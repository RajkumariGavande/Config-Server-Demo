server:
  port: 8081

spring:
  application:
    name: payment-service

  datasource:
    url: jdbc:mysql://localhost:3306/testdb
    username: root
    password: Gavande@123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update     # create | create-drop | update | validate
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

  sql:
    init:
      mode: always

# Custom message
msg: "Welcome to Spring Boot with Config server"
 
